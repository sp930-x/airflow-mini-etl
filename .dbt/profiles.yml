weather_dbt:
  outputs:
    dev:
      dbname: airflow
      host: postgres
      pass: airflow
      port: 5432
      schema: dbt
      threads: 1
      type: postgres
      user: airflow
  target: dev
